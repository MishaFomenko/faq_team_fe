export type Inputs = {
  email: string;
  full_name: string | number | readonly string[] | undefined;
};

export type Props = {
  email_value: string;
  full_name: string | number | readonly string[] | undefined;
  id: string;
};
